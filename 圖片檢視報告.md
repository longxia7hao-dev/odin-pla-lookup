# 網站圖片全面檢視報告
稽核日期：2026-08-03｜稽核方式：490 張全部產生縮圖總覽，逐張目視

## 總覽
| 項目 | 數字 |
|---|---|
| 資料總筆數 | 645 |
| 有圖 | 490（76%）|
| 無圖（占位） | 155 |
| 破圖（路徑存在但檔案遺失） | 0 ✅ |

### 圖片來源分布
| 來源 | 張數 | 品質評估 |
|---|---|---|
| 舊有（含 Grok 早期批次） | 191 | 良好，但混有標題卡/廣告圖 |
| 維基指定主圖（我的嚴格抓取） | 134 | 最佳，已全數驗證 |
| Wikimedia Commons（我的抓取） | 69 | 良好，含少數識別線稿 |
| 百度百科（Grok 抓取） | 96 | 良好，但混有資訊圖/標題卡 |

---

## 🔴 A 級：建議立即移除（明確錯誤，14 筆）

### A-1 完全無關的圖片（5 筆）
| id | 裝備 | 目前圖片 |
|---|---|---|
| `ft-series` | 飛騰系列制導炸彈 | **QQ瀏覽器 DeepSeek R1 廣告圖** |
| `type-654-arv` | 654式裝甲搶救車 | **美軍 M113**（車身有白色星形標誌）|
| `zdk-03` | ZDK-03 預警機 | **西方 P-3 級巡邏機**（機身 AIR FORCE 字樣）|
| `type-002` | 山東艦 | **部隊臂章圖案**（非艦艇照片）|
| `asn-206` | ASN-206 無人機 | **全黑圖**（壞檔）|

### A-2 「藍底大字標題卡」非實物照（5 筆）
`ba-9`（AKD-9）、`dwl-002`（DWL002 雷達）、`h-20`（轟-20）、`type-120-radar`（K/LLQ-120雷達）、`wz-9`（无侦9）
> 皆為同一來源的 AI 生成標題圖，只有大字與卡通背景，無裝備實體。

### A-3 中文標題卡／新聞截圖（4 筆）
| id | 問題 |
|---|---|
| `type-84-minelayer` | 「84式24管火箭布雷車」大字標題卡 |
| `df-15` | 「东风15 变色弹头 特种钻地」標題卡 |
| `y-8g` | 日本 C-2 運輸機相關**新聞截圖** |
| `sh5-105` | **模型包裝盒**繪圖 |

---

## 🟠 B 級：建議替換（可辨識但非實照，9 筆）

| 類型 | id | 說明 |
|---|---|---|
| 資訊圖卡 | `df-21d`、`type-095` | 中文諸元說明圖，含大量文字 |
| 文字疊加 | `type-052c`、`type-003`、`df-5c` | 照片上壓「052C」「突破」「新突破」等大字 |
| CG 渲染 | `kj-3000`、`type-052`、`type-004-rumor` | 電腦繪圖非實照（004 屬傳聞型，可接受）|
| 疑似模型 | `q-5`、`su-27ubk`、`su-30mk2`、`j-10s`、`z-15` | 藍/藍紅塗裝，疑為模型或 CG |
| 民用塗裝 | `z-8`、`z-8g` | 橘白民用救援塗裝，非軍用型 |

---

## 🟡 C 級：可接受但不理想

### C-1 重複圖（31 組、67 筆）
同一張圖被多個變體共用，**遊戲出題時會造成兩個選項圖片一樣**：

| 共用同一張圖 |
|---|
| `ztz-96` = `ztz-96a` = `ztz-96b` |
| `type-093` = `type-093a` = `type-093b` |
| `type-039a` = `type-039b` = `type-039c` |
| `zbd-04` = `zbd-04a` = `zbd-04a-at` |
| `hq-9a` = `hq-9b` = `hhq-9` |
| `zbd-05` = `ztd-05`（步戰車 vs 突擊砲，**應不同**）|
| `qbz-191` = `qbu-191`（步槍 vs 精確射手槍，**應不同**）|
| `pll-09` = `pcl-161`（不同火砲，**應不同**）|
| `zsl-92` = `type-92b`、`type-81-rifle` = `type-81-lmg` |

### C-2 尺寸過小（43 筆，多為 330×220）
`ztz-96` 系列、`zbd-05`、`plz-07`、`phl-03`、`pgz-09`、`hq-17`、`eq2058` 等，放大檢視會模糊。

### C-3 識別線稿（8 筆）
`type-072iii`、`type-074a`、`type-082`、`type-082i`、`type-082ii`、`type-022`、`type-021`、`h-5`
> 黑色剪影識別圖，資訊性尚可，但非照片。

---

## ✅ 品質良好者（約 430 張）
維基指定主圖批次（134 張）與 Commons 批次（69 張）品質最佳；百度批次的艦艇、戰機、火砲、飛彈車多為正確實照。

---

## 建議處理順序
1. **立即移除 A 級 14 筆** → 改回占位圖（寧可沒圖不放錯圖）
2. **B 級 9 筆**：優先找替代圖，找不到就移除
3. **C-1 重複圖**：優先處理「應該不同卻共用」的 4 組（`zbd-05`/`ztd-05`、`qbz-191`/`qbu-191`、`pll-09`/`pcl-161`、`zsl-92`/`type-92b`）
4. C-2、C-3 可留待日後補更好的圖
